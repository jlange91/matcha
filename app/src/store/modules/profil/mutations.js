export const SET_USER_PROFIL = (state, profil) => {
    state.profil = profil
}

export const CLEAR_USER_PROFIL = (state) => {
    state.profil = null
}

export const SET_USER_LOCATION = (state, location) => {
    state.location = location
}

export const CLEAR_USER_LOCATION = (state) => {
    state.location = null
}